import helpers from '../utils/helperFunctions.mjs'; (function () { function a (a) { const b = a.target.checked; const c = a.target.id; b && !f.includes(c) && f.push(c), !b && f.includes(c) && (f = f.filter(a => a !== c)) } function b () { c({ cssGetters: f }, () => { e('Settings Saved Successfully', 'The settings have been updated and will take effect the next time you use the extension.') }) } const { setItem: c, getItem: d, createNotification: e } = helpers; let f; const g = document.getElementById('save'); const h = document.getElementById("backgroundColor"); const i = document.getElementById("color"); const j = document.getElementById("fontFamily"); const k = document.getElementById("fontSize"); const l = document.getElementById('fontWeight');[h, i, j, k, l].forEach(b => { b.addEventListener('change', a) }), d('cssGetters', ({ cssGetters: a }) => { f = a, f.forEach(a => { document.getElementById(a).checked = !0 }) }), g !== null && g.addEventListener('click', b) })()
